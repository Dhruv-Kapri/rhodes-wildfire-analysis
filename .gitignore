# --- Python / uv ---
.venv/
__pycache__/
*.py[cod]
.python-version.local

# --- Jupyter ---
.ipynb_checkpoints/
*.ipynb_checkpoints

# --- Data (fetched via Figshare + STAC; see data/README.md) ---
# These are large (>1 GB total) and must not be committed to git.
/data/*
!/data/README.md

# --- Notebook-generated artifacts (regenerated by running the notebooks) ---
notebooks/*.tif
notebooks/*.tiff
notebooks/*.json

# --- OS / editor cruft ---
.DS_Store
.idea/
.vscode/
*.swp
